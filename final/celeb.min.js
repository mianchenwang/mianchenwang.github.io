/*
var datafile = "data.json";
var info = [];
//const info = document.getElementById("text");  

Image.addEventListener("click",showInfo);

function preload(file) {
    //load json data file
    $.getJSON(file, function(data) { 
        //loop through data to get image paths       
        $.each(data.celebs, function( i, obj ) {
            //preload images
            var img = new Image();
            img.src = obj.image;
            console.log(obj.image);
        }); 
    });
}


//call the preload function    
preload(datafile);


$(function() { 

    //get file again 
    $.getJSON(datafile, function(data) { 
        //store json data in array
        info = data.celebs;
        //loop through array to get content
        {
          $("#informationbox").append("<img  src="+obj.image+"><div class='text'><span class='title'>"+obj.image+"</span><span class='caption'>"+obj.text+"</span></div></li>");
        });
    });
});
*/